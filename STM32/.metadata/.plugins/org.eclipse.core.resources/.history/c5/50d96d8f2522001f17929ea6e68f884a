/*
 * traj.h
 *
 *  Created on: May 30, 2024
 *      Author: 23877
 */

#ifndef TRAJ_H_
#define TRAJ_H_

#define COLS 8

// 定义一个结构体来存储 CSV 解析后的数据
typedef struct {
    float **data;
    uint16_t rows;
    uint16_t cols;
} CSVData;

int parseLine(char *line, int *row, int maxCols);
CSVData Read_CSV(const char *filename);
void Free_CSVData(CSVData csvData);

void Start_Local_Traj(unsigned char traj_type);
void Start_Global_Traj(unsigned char traj_type);

#endif /* TRAJ_H_ */
